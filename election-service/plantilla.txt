# ============================================================
# ELECTION SERVICE
# Entorno local para quarkusDev
# ============================================================

DB_URL=jdbc:postgresql://localhost:54323/sisve_election?currentSchema=election_schema
DB_USERNAME=election_app_user
DB_PASSWORD=election_password_local_2026

QUARKUS_HTTP_PORT=8082

CONSUL_HOST=localhost
CONSUL_PORT=8500

AUDIT_SERVICE_URL=http://localhost:8084

QUARKUS_DATASOURCE_DEVSERVICES_ENABLED=false